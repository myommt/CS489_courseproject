apiVersion: apps/v1
kind: Deployment
metadata:
  name: dentalsurgeryapp-deployment
  labels:
    app: dentalsurgeryapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dentalsurgeryapp
  template:
    metadata:
      labels:
        app: dentalsurgeryapp
    spec:
      containers:
        - name: dentalsurgeryapp
          image: myommt/dentalsurgeryapp2-aks:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 8083
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
      # Optional service account, tolerations, nodeSelector can be added here
